'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _util = require('util');

var _loggerProxy = require('../logs/logger-proxy');

var _loggerProxy2 = _interopRequireDefault(_loggerProxy);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var EventsUtil = {};

EventsUtil.getEventLog = function (args) {
  var argString = '';

  try {
    argString = (0, _util.inspect)(args);
  } catch (e) {
    _loggerProxy2.default.logger.warn('Events:util#getEventLog --> ' + e);
  }

  return argString;
};

EventsUtil.getScopeLog = function (scope) {
  var scopeString = '';

  if (scope) {
    if (scope.file && scope.function) {
      scopeString += scope.file + ':' + scope.function + '->';

      return scopeString;
    }
    if (scope.file) {
      scopeString += scope.file + '->';
    }
    if (scope.function) {
      scopeString += scope.function + '->';
    }
  }

  return scopeString;
};

exports.default = EventsUtil;
//# sourceMappingURL=util.js.map
